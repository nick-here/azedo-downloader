(function () {
	// copy and paste into browser's console

	// Queries used to access poduct's download button
	const nodeQueries = {
		navbarLink: '#wide-nav .nav-top-link',
		product: '.product-small a',
		btn: '#somdn-form-submit-button',
	};

	document.querySelector(nodeQueries.navbarLink).click();
})();
